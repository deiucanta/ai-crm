get:
  summary: List all people
  description: Retrieves a list of people from the system.
  parameters:
    - $ref: '../components/parameters/page.yaml'
    - $ref: '../components/parameters/size.yaml'
  responses:
    200:
      description: A list of people
      content:
        application/json:
          schema:
            $ref: '../components/schemas/person_list.yaml'
